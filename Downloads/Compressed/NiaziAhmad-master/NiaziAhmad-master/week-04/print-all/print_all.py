# - Create a variable named `numbers`
#   with the following content: `[4, 5, 6, 7]`
# - Print all the elements of `numbers`
numbers = [4, 5, 6, 7]
for i in numbers:
    print(i, end=" ")
# also we can print like this:
print()
print(f"we can print it like this too : {numbers}")
